# Start your application with setting venv:

1. Run this bash scripts

```bash
python -m venv .venv
```

- active in git script is

```bash
source .venv/bin/activate
```

- active on powershell

```bash
.venv\bin\Activate.ps1
```

2. install dep

```bash
pip install python-dotenv
```
